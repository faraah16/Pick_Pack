<?xml version="1.0" encoding="utf-8"?>

<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#dfdfdf">

    <!-- ================= IMAGE BACKGROUND ================= -->
    <ImageView
        android:id="@+id/bgImageOrders"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:alpha="0.49"
        android:clickable="false"
        android:focusable="false"
        android:importantForAccessibility="no"
        android:scaleType="centerCrop"
        android:src="@drawable/img" />

    <!-- ================= TON CODE ORIGINAL (INCHANGÉ) ================= -->
    <ScrollView
        android:id="@+id/order_list_root"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/transparent"
        android:backgroundTint="#dfdfdf"
        android:padding="16dp">

        <LinearLayout
            android:id="@+id/order_list_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- Titre -->
            <TextView
                android:id="@+id/tvTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="16dp"
                android:fontFamily="sans-serif-black"
                android:text="Orders"
                android:textSize="20sp"
                android:textStyle="bold" />

            <!--
              IMPORTANT :
              La liste des commandes est ajoutée DYNAMIQUEMENT
              depuis OrderListFragment (TextView créés en Java)
            -->

        </LinearLayout>

    </ScrollView>

</FrameLayout>
